{% trans_default_domain 'ShopBundle' %}
<h3 class="text-center">{{ 'layout.lastseen.header'|trans }}</h3>
<div class="col-md-12">
    {% for product in products %}
        {{ include('@Shop/Partials/product_preview.html.twig') }}
    {% endfor %}
</div>
